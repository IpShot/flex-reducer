const mainConfig = {
  displayName: 'ReactDOM',
  testMatch: ['<rootDir>/test/**/*.test.js'],
  coverageDirectory: './coverage/',
  collectCoverage: true
}

module.exports = {
  projects: [mainConfig]
}
